<nav class="flex justify-between items-center bg-blue-400 text-blue-100 text-xl px-4 py-2">
  <a href="#">
    <i class="fab fa-trello"></i> tello
  </a>
  <div>
    <% if user_signed_in? %>
      <span class="mr-2"><%= current_user.email %></span>
      <%= link_to '登出', destroy_user_session_path, method: 'delete',class:'text-l px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200' %>
    <% else %>
      <%= link_to '登入', new_user_session_path, class:'px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200' %>
      <%= link_to '註冊', new_user_registration_path, class:'px-3 py-2 bg-blue-300 rounded-sm hover:bg-blue-200' %>
    <% end %>
  </div>
</nav>